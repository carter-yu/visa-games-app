# Codex starting prompt

Use this as the first implementation task after opening the repository in Codex:

> Read `AGENTS.md`, `README.md`, `docs/ground-rules.md`, `docs/decisions/0001-native-kiosk-boundary.md`, `PROGRESS.md`, and `phases/phase-0-native-shell.md` before changing files. Implement only Phase 0. Keep dependencies minimal and use Swift/SwiftUI with AppKit only where native window/presentation APIs require it. First create the smallest buildable macOS project and tests for the state/timer/persistence behavior. Then implement the native shell. Do not add YouTube or port tracing/connect tasks yet. Run tests and build, update `PROGRESS.md`, and report any macOS kiosk escape paths that cannot be controlled by the app itself.
